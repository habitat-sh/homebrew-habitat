steps:

  - label: "Install Habitat from Tap"
    command:
      - .expeditor/scripts/verify/install_from_tap.sh
    expeditor:
      executor:
        macos:
          os-version: "12"
          inherit-environment-vars: true
